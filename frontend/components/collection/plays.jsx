import React from 'react';
// import WaveItem from 'wave_item';

const Plays = ({tracks, nowPlaying}) => {

  return(
    <div>
    </div>
  );
};
// {tracks.map( track => <WaveItem track={track} />)}

export default Plays;
